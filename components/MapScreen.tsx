// This component is currently unused as navigation is handled in SceneView. 
// However, we keep a placeholder to prevent build errors if referenced elsewhere, 
// or for future expansion of a "World Map" view.

import React from 'react';

export const NavigationMap = () => {
  return <div className="hidden">Map View</div>;
};